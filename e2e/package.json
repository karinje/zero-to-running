{
  "name": "zero-to-running-e2e",
  "version": "1.0.0",
  "description": "E2E tests for Zero-to-Running Developer Environment",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}

